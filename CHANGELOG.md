# Changelog

## 0.7.0 — 2026-09-22

桌面悬窗：一个半透明的绿色水量球浮在桌面最上层，水量高度即内存占用比例，悬停可看系统读数，点击即释放内存。

### Added

- 桌面悬窗（macOS 与 Windows 同一套前端，后端按平台各自取数）：
  - 半透明绿色圆形水量球，水量高度对应内存占用比例，球心显示当前占用百分比（四舍五入后的读数 ≥ 90% 时转为橙红色，面板「内存」一行同步变色），水面为双层反向流动的波浪；内存读数为 0 时球内为空
  - 悬停展开信息面板：内存占用、CPU 占用与温度、风扇转速、磁盘占用、网络上下行速率，面板显示在球的上方或下方（依屏幕空间自动选边）
  - 面板与悬窗（含提示文案）随界面语言切换即时生效，中英文皆可：语言状态在窗口之间广播，悬窗与状态栏等独立窗口不再停留在启动时的语言
  - 点击球体释放内存：水面正对屏幕转起一道漩涡——水中浮出同心圆环与一道螺旋，越靠圆心转得越快，两层波浪也被带着一起涌动，其中涌动恰好收在起止相同的位置；漩涡只是渐显渐隐地画在水面上，水线始终水平、水量与读数都不受影响；过程中显示「释放中」，完成后显示本次释放量；无传感器数据的机器对应项显示「—」
  - 双击球体打开 Windle 主界面：单击会先等 260ms 确认没有第二下再释放内存，所以双击只打开窗口、不会顺带释放一次
  - 右键隐藏，按住拖动可移动；显隐状态与所在位置写入 `floating.json` 跨启动保留，显示时若记录的位置已不在任何显示器上（如拔掉外接屏）则回落到当前显示器右下角；位置按物理像素与显示器范围比对，缩放屏（Retina）上不会把有记录的位置误判为不在屏内
  - 侧边栏按钮与托盘菜单项两个开关入口，托盘勾选状态、侧边栏按钮状态与实际显隐始终同步（包括右键隐藏与窗口关闭按钮）
  - 球体直径为原来的 1/4（33px），面板保持原宽度；面板比球宽，所以展开时窗口在横向与纵向都要伸长，球锚定在不动的那一侧角上，球本身不位移；窗口始终与球和面板的绘制内容等大，不会挡住看不见的点击
  - 悬窗只取自己需要的读数（不含进程表），磁盘容量按 TTL 复用同一次采样，隐藏时不轮询；后台刷新，展开/收起与拖动都不断流
  - macOS 依赖 `macos-private-api` 才能做到真正的圆角透明；`acceptFirstMouse` 让未聚焦的球第一下点击即生效；窗口在全部工作区可见

### Fixed

- 悬窗拖不动：`core:default` 只授予窗口的读取权限（`outer-position`、`scale-factor`、`current-monitor`），`set-position` 并不在其中，拖动时 `setPosition` 被 ACL 拒绝、又被前端的兜底 `catch` 吞掉，表现就是「能点不能拖」；已补上 `core:window:allow-set-position`
- 快速拖动后位置可能不落盘：窗口移动事件的写入是限流的（每秒最多一次），一次不到一秒的拖动只会记下开头那一帧，下次启动就把球放回了拖动的起点；现在拖动结束时会显式保存一次球所在的位置
- 打开别的应用后悬窗不再响应 hover（macOS）：系统只把鼠标跟踪事件投给当前活跃的应用，后台窗口收不到 `pointerenter`/`pointerleave`，表现就是鼠标移到球上也不展开面板；现在由后端定时读取全局鼠标位置（约每 120ms 一次，仅在悬窗显示时），指针进入或离开悬窗时通知前端，面板在前台是别的应用时同样能展开；这段监看逻辑两平台共用，Windows 本就向非活跃窗口投递悬停事件，与既有行为一致
- 悬窗面板的提示文案会伸进球体覆盖的右下角下面（球缩到 33px 后尤其明显，最后一个词被球截断）：提示行预留出球的宽度，中英文都收在两行以内
- Windows 的 CPU 温度与风扇转速此前恒为空（主界面与状态栏都是「—」）：改为经 WMI 硬件监视类读取，无硬件监视服务的机器回落到 ACPI 热区；读数在后台刷新，温度与风扇低频采样、查询整体失败后更长时间退避，并对读数做范围校验，避免固件怪值（-40 °C 或数百万 RPM）上屏
- Windows 系统优化执行时不断弹出 cmd 黑窗：GUI 进程本身没有控制台，直接启动控制台程序会各分配一个新窗口，现在所有子进程都以 `CREATE_NO_WINDOW` 创建
- Windows 系统优化长时间没有结果：进度事件改为按任务上报（当前任务索引 + 该任务单独计时），界面显示正在执行哪一步、该步已用多久，而不是整批任务一直空转

### Changed

- 应用数据目录收敛到 `utils::platform::app_support_dir()`：macOS 为 `~/Library/Application Support/Windle`，Windows 为 `%LOCALAPPDATA%\Windle`，历史记录与悬窗位置共用同一处
- 新增 `utils/process.rs`：统一封装子进程的 `CREATE_NO_WINDOW`，Windows 上不再有黑窗，其他平台为空操作

## 0.6.0 — 2026-09-18

Windows 版本发布：功能与界面与 macOS 版一致，同一套前端代码，后端按平台分支实现。

### Added

- Windows 平台支持（全部 10 个模块可用）：
  - 深度清理：12 个分类全部落地，缓存/日志/应用垃圾/浏览器缓存等按 Windows 路径展开；系统日志覆盖 `%SystemRoot%\Logs`、`Minidump`、`Temp`、WER 报告与 LiveKernelReports；开发者缓存分类扫描 NuGet / npm / pip / Yarn / cargo / gradle 与 Visual Studio 缓存
  - 智能卸载：注册表枚举已安装程序（HKCU / HKLM / WOW6432Node 两个视图），静默调用厂商卸载程序（MSI / NSIS），随后清理残留的应用数据、开始菜单快捷方式与 `Software` 注册表项
  - 启动项：注册表 Run 键与「启动」文件夹的读取与启用/禁用
  - 系统优化：10 个 Windows 任务（内存回收、DNS 刷新、清理临时文件、清理 Windows Update 缓存、重建搜索索引、刷新图标缓存、SFC 系统文件校验、DISM 系统映像修复、系统盘 TRIM/碎片整理、系统盘检查），其中 7 个需要提升权限
  - 安装包清理：识别 `.exe` / `.msi` / `.msix` / `.appx` / `.msu`；挂载镜像弹出改为对虚拟光驱发送 eject IOCTL
  - Docker 清理：通过 `\\.\pipe\docker_engine` 命名管道直连 Docker Engine API（`DOCKER_HOST` 支持 `npipe://`）
  - 窗口与托盘：无边框窗口 + 界面内自绘的最小化 / 最大化 / 关闭按钮（46px 宽、悬停高亮、关闭按钮红色），标题栏区域可拖拽、双击最大化；单实例运行与系统托盘
- 权限守卫的 Windows 等价实现（`utils/permissions/windows.rs`）：
  - 19 个系统前缀保护（`%SystemRoot%`、`%ProgramFiles%`、`%ProgramData%\Microsoft\Crypto` 等）与 26 个只允许清空的目录（用户主目录、`AppData` 容器、`~\.ssh`、`~\.aws` 等）
  - 10 个豁免子树的严格后代可清理（`%SystemRoot%\Temp`、`SoftwareDistribution\Download` 等），子树根目录本身仍受保护
  - 路径比较大小写不敏感、剥离 canonicalize 产生的 `\\?\` 前缀，杜绝大小写与前缀变体绕过
  - 无 TCC 类权限：完全磁盘访问总是「已授权」；受 UAC 保护的位置由 `needs_elevation` 判定并提示提升
- 前端平台适配：`platform_info` 命令 + 首帧渲染前解析平台；Windows 下窗口按钮、拖拽区域与部分文案（`${key}.windows` 覆盖机制）自动切换
- CI：`.github/workflows/windows-build.yml`（windows-latest 上执行类型检查、`cargo test`、构建 NSIS 安装包并上传产物）
- `scripts/check-windows.mjs`：在 macOS 上以 `cargo check --target x86_64-pc-windows-msvc --all-targets` 交叉检查 Windows 代码路径

### Changed

- 后端按平台拆分：`commands/clean`、`commands/uninstall`、`commands/optimize` 与 `utils/permissions` 拆为 `mod.rs` + `macos.rs` + `windows.rs`，共享逻辑与命令签名保持不变
- Tauri 配置拆分：`tauri.macos.conf.json`（Overlay 标题栏、`.app` / `.dmg`）与 `tauri.windows.conf.json`（无边框窗口、NSIS、中英文安装界面）
- macOS 行为保持不变：窗口 chrome、文案与守卫逻辑均按平台分支，macOS 侧渲染结果与 0.5.0 一致

## 0.5.0 — 2026-09-15

### Fixed

- 智能卸载修复应用本体删不掉的问题：此前前端只上报残留文件路径、后端将其视为完整删除清单，导致 `.app` 包从未被删除却提示「已完成」（应用有残留时必现）
- 智能卸载修复取消勾选全部残留时误删的问题：空清单此前被解释为「全部删除」，未勾选的残留也会被删除
- 智能卸载结果如实反馈：仅当应用包确认删除后才从列表移除并显示成功卡片；存在失败项时显示警告卡片与失败原因
- 智能卸载释放空间改为按实际删除字节统计（原按卸载前的计划大小统计）
- 智能卸载执行后会刷新该应用的残留缓存，重试时不再把已删除项报为失败

## 0.4.0 — 2026-09-01

### Added

- 深度清理新增「应用垃圾」分类（五个二级分组）：
  - 应用缓存、网站数据（HTTPStorages）、保存的应用状态、WebKit 数据、沙盒应用缓存
- 深度清理支持二级分类（分组）粒度勾选：
  - 全局 / 分类 / 分组 / 单项四级联动，可只清理指定的二级分类
- 日志分类新增二级分组：用户日志、崩溃报告、系统日志、沙盒应用日志
- 深度清理的分类、分组、描述与风险徽章支持中英文切换（12 个分类、11 个二级分组，扫描进度与相对时间同步本地化）
- 系统日志新增统一日志归档来源 /private/var/db/diagnostics（归档文件级全量展示，无年龄过滤，谨慎风险标记）

### Changed

- 日志分类补充系统运行时日志与报告来源：/Library/Logs、系统级与用户级 CrashReporter、~/Library/Containers 与 Group Containers 沙盒应用日志
- 日志分类权限豁免机制：/private/var/db/diagnostics 子树严格后代（全深度）可扫描与清理（根目录与其余 /private/var/db 路径仍受保护）
- 状态栏快速清理收敛删除范围：仅永久删除安全风险项，不再触碰 Caution 级系统项与沙盒应用日志

### Fixed

- 应用垃圾清理排除浏览器与框架级 WebKit 数据（Databases/WebPush）及 .binarycookies 登录数据，避免登出网页账号
- 系统级 DiagnosticReports 统一归入崩溃报告分组
- 清理后可清理总量统计与分类卡片合计口径一致
- 修复诊断归档目录因 logd 持续写入导致目录修改时间恒新、7 天过滤永不命中而扫描为空的问题（改为归档文件级扫描）

## 0.3.0 — 2026-08-16

### Added

- Docker 容器清理模块（侧栏新入口「容器清理」）：
  - Docker Desktop 环境检测：未安装 / 未运行 / 就绪三态；未运行时一键启动并轮询就绪（2s 间隔，90s 超时兜底）
  - 空间概览：镜像 / 容器 / 卷 / 构建缓存四类总量、活跃量与可回收量（精确字节，由 /system/df 明细推导）
  - 五类扫描与清理：悬空镜像（默认勾选）、未使用镜像、已停止容器（默认勾选）、未使用卷（高危红色标记）、构建缓存（按最后使用时间窗口：全部 / 72h / 7 天 / 30 天，默认全部）
  - 一键组合清理：预览选中规模 → 危险色确认对话框（勾选卷时附持久化数据警示）→ 进度事件 → 结果报告（释放字节 / 删除数 / 失败清单）
  - 强制安全校验：运行中容器、使用中镜像 / 卷 / 构建缓存在 Rust 端二次拒删，不依赖前端勾选
  - 清理历史计入操作记录（operation: docker）；侧栏“本次会话已释放”随结果累加
- Rust：新增 `commands/docker.rs` —— 系统 curl（/usr/bin/curl --unix-socket）直连 Docker Engine API v1.43；socket 探测顺序 DOCKER_HOST(unix://) → /var/run/docker.sock → ~/.docker/run/docker.sock；零新增依赖
